---
devices:
  username: vagrant
  password: vagrant

ios:
  platform: ios
  groups:
    - devices

eos:
  platform: eos
  groups:
    - devices


junos:
  platform: junos
  username: vagrant
  password: NTC123
# add password to vagrant user which had public key login
# root@vagrant-junos-test> configure
# Entering configuration mode

# [edit]
# root@vagrant-junos-test# edit system login

# [edit system login]
# root@vagrant-junos-test# edit user vagrant

# [edit system login user vagrant]
# root@vagrant-junos-test# set authentication plain-text-password
# New password:
# error: require change of case, digits or punctuation

# [edit system login user vagrant]
# root@vagrant-junos-test# set authentication plain-text-password
# New password:
# Retype new password:

# [edit system login user vagrant]
# root@vagrant-junos-test# commit
# commit complete

# [edit system login user vagrant]
# root@vagrant-junos-test#
